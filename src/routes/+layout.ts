// remove server render completely because we store all data in localStorage
// TODO: Change this two lines accordingly (disable prerender or make proper ssr) after we have a backend
export const prerender = true
export const ssr = false
